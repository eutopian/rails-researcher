<%= form_for @review, url: article_reviews_path, html: {multipart: true} do |f| %>
  <%= f.label :document %>
  <%= f.file_field :document %>
 	<%= f.hidden_field :article_id, value: params[:article_id] %>
  <%= f.hidden_field :reviewer_id, value: @user.id %>
  <br>
  <%= f.submit %>
<% end %><br><br>

Reviewed by: 
<% Review.where(article_id: params[:article_id]).each do |rev| %>
	<%= rev.reviewer.first_name if rev.reviewer %>
	<% end %>